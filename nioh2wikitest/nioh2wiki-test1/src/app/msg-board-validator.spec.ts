import { MsgBoardValidator } from './msg-board-validator';

describe('MsgBoardValidator', () => {
  it('should create an instance', () => {
    expect(new MsgBoardValidator()).toBeTruthy();
  });
});
